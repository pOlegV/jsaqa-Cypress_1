{
  "name": "jsaqa_dz_6",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test:viewport2": "npx cypress run ./e2e/login.cy.js --config-file cypress.env.config.js",
    "chrome": "npx cypress run --browser chrome",
    "electron": "npx cypress run --browser electron",
    "chrome:head": "npx cypress run --browser chrome --headed",
    "electron:head": "npx cypress run --browser electron --headed"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "cypress": "^12.17.2"
  }
}
